openapi: 3.0.3
info:
  title: TCSS 460 Group Project API
  description: |
    **Team:** Group 4
    **Deployed URL:** https://group-project-backend-group-4.onrender.com

    A RESTful API for a move and TV review community.
  version: 0.1.0

servers:
  - url: http://localhost:3000
    description: Local development
  - url: https://group-project-backend-group-4.onrender.com
    description: Production deployment

tags:
  - name: General
    description: General-purpose routes
  - name: Movies
    description: Routes used with Movies
  - name: TV Shows
    description: Routes used with TV Shows
  - name: Reviews
    description: Routes used with persisted user reviews
  - name: Ratings
    description: Routes used with persisted user ratings
  - name: Issues
    description: Routes for reporting and tracking issues

paths:
  /heartbeat:
    get:
      tags: [General]
      summary: Heartbeat
      description: Returns the health status of the API.
      responses:
        '200':
          description: API is alive and running.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HeartbeatResponse'
  /api/movies/{movie_id}:
    get:
      tags: [Movies]
      summary: Movie Details
      description: Provides details on the provided movie, based on it's provided id
      parameters:
        - name: movie_id
          in: path
          required: true
          schema:
            type: integer
          description: TMDB movie ID
      responses:
        '200':
          description: Successfully get back movie details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovieDetail'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: movie_id doesn't exist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/movies/popular:
    get:
      tags: [Movies]
      summary: Popular Movies
      description: Returns content sorted by popularity using the TMDB discover endpoint. Showcases widely appealing movies.
      parameters:
        - name: language
          in: query
          required: false
          schema:
            type: string
            default: 'en-US'
          description: ISO-639-1 language code (e.g., 'en-US')
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: sort_by
          in: query
          required: false
          schema:
            type: string
            default: 'desc'
            enum: ['desc', 'asc']
      responses:
        '200':
          description: Successfully retrieved a list of popular movies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MovieListItem'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/movies/featured:
    get:
      tags: [Movies]
      summary: Featured Movies
      description: Returns featured movies based on community interactions, sorted by most-reviewed or top-rated. Items whose TMDB data is unavailable (404) are silently omitted from the response. Results are only updated every 5 minutes, so new reviews/ ratings may take some time to be reflected.
      parameters:
        - name: sort
          in: query
          required: false
          schema:
            type: string
            enum: [most-reviewed, top-rated]
            default: top-rated
          description: The sorting metric for featured movies.
      responses:
        '200':
          description: Successfully retrieved a list of featured movies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FeaturedMovie'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/movies/search:
    get:
      tags: [Movies]
      summary: Search Movies
      description: Search movies by title.
      parameters:
        - name: title
          in: query
          required: true
          schema:
            type: string
          description: Search term for movie titles.
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
            maximum: 1000
          description: Page number for TMDB pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 50
          description: Maximum number of results returned in this response.
      responses:
        '200':
          description: Successfully retrieved movie search results.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Search results not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/tv/search:
    get:
      tags: [TV Shows]
      summary: Search TV Shows
      description: Search TV shows by title.
      parameters:
        - name: title
          in: query
          required: true
          schema:
            type: string
          description: Search term for TV show titles.
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
            maximum: 1000
          description: Page number for TMDB pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 50
          description: Maximum number of results returned in this response.
      responses:
        '200':
          description: Successfully retrieved TV search results.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Search results not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/tv/{series_id}:
    get:
      tags: [TV Shows]
      summary: TV Show Details
      description: Provides details on the provided TV Show, based on it's provided id
      parameters:
        - name: series_id
          in: path
          required: true
          schema:
            type: integer
          description: TMDB series ID
      responses:
        '200':
          description: Successfully get back TV details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TVShowDetail'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: series_id doesn't exist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/tv/featured:
    get:
      tags: [TV Shows]
      summary: Featured TV Shows
      description: Returns featured TV shows based on community interactions, sorted by most-reviewed or top-rated. Items whose TMDB data is unavailable (404) are silently omitted from the response. Results are only updated every 5 minutes, so new reviews/ ratings may take some time to be reflected.
      parameters:
        - name: sort
          in: query
          required: false
          schema:
            type: string
            enum: [most-reviewed, top-rated]
            default: top-rated
          description: The sorting metric for featured TV shows.
      responses:
        '200':
          description: Successfully retrieved a list of featured TV shows
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FeaturedTVShow'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/tv/popular:
    get:
      tags: [TV Shows]
      summary: Popular TV Shows
      description: Returns content sorted by popularity using the TMDB discover endpoint. Showcases widely appealing TV shows.
      parameters:
        - name: language
          in: query
          required: false
          schema:
            type: string
            default: 'en-US'
          description: ISO-639-1 language code (e.g., 'en-US')
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: sort_by
          in: query
          required: false
          schema:
            type: string
            default: 'desc'
            enum: ['desc', 'asc']
      responses:
        '200':
          description: Successfully retrieved a list of popular TV shows
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TVShowListItem'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/reviews:
    get:
      tags: [Reviews]
      summary: List Reviews
      description: Returns paginated reviews with optional filters.
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
          description: Page number.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 50
          description: Number of reviews per page.
        - name: userId
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
          description: Filter by author user id.
        - name: mediaId
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
          description: Filter by persisted media id.
        - name: tmdbId
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
          description: Filter by TMDB id. Must be provided with `type`.
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [MOVIE, TV_SHOW]
          description: Media type for `tmdbId` filter. Must be provided with `tmdbId`.
      responses:
        '200':
          description: Reviews retrieved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewListResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [Reviews]
      summary: Create Review
      description: Creates a review for the authenticated user.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tmdbId, type, body]
              properties:
                tmdbId:
                  type: integer
                  minimum: 1
                type:
                  type: string
                  enum: [MOVIE, TV_SHOW]
                title:
                  type: string
                body:
                  type: string
      responses:
        '201':
          description: Review created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: User already reviewed this media.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/reviews/me:
    get:
      tags: [Reviews]
      summary: Get My Reviews
      description: Returns a paginated list of the authenticated user's reviews, ordered by most recent.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
            maximum: 1000
          description: Page number.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 50
          description: Number of reviews per page.
      responses:
        '200':
          description: Successfully retrieved personal reviews.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewListResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/reviews/{id}:
    get:
      tags: [Reviews]
      summary: Get Review By ID
      description: Returns one review by review identifier.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Review retrieved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewWithRelations'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Review not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags: [Reviews]
      summary: Update Review
      description: Updates a review. Only the review owner can modify their own review.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                body:
                  type: string
      responses:
        '200':
          description: Review updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not allowed to modify this review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Review not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Reviews]
      summary: Delete Review
      description: Deletes a review. The authenticated user must be the review owner, or have the Admin role or above (Admin, SuperAdmin, Owner).
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '204':
          description: Review deleted successfully. No content returned.
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not allowed to delete this review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Review not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/ratings:
    get:
      tags: [Ratings]
      summary: List Ratings
      description: Retrieve a paginated list of ratings. Filterable by user, media, or TMDB ID and type.
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 50
        - name: userId
          in: query
          required: false
          schema:
            type: integer
        - name: mediaId
          in: query
          required: false
          schema:
            type: integer
        - name: tmdbId
          in: query
          required: false
          schema:
            type: integer
          description: Filter by TMDB id. Must be provided with `type`.
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [MOVIE, TV_SHOW]
          description: Media type for `tmdbId` filter. Must be provided with `tmdbId`.
      responses:
        '200':
          description: A paginated list of ratings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingListResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [Ratings]
      summary: Create Rating
      description: Submit a new rating. If the media doesn't exist, it will be resolved and created automatically.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tmdbId, type, score]
              properties:
                tmdbId:
                  type: integer
                  example: 550
                type:
                  type: string
                  enum: [MOVIE, TV_SHOW]
                  example: MOVIE
                score:
                  type: integer
                  example: 5
                  minimum: 1
                  maximum: 5
      responses:
        '201':
          description: Rating successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid Token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflict - User has already rated this specific media
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/ratings/me:
    get:
      tags: [Ratings]
      summary: Get My Ratings
      description: Returns a paginated list of the authenticated user's ratings, ordered by most recent.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
            maximum: 1000
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 50
      responses:
        '200':
          description: Successfully retrieved personal ratings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingListResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/ratings/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: The primary key of the rating
    get:
      tags: [Ratings]
      summary: Get Rating by ID
      responses:
        '200':
          description: Successfully retrieved the rating
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingWithRelations'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags: [Ratings]
      summary: Update Rating
      description: Updates a rating. Only the rating owner can modify their own rating.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [score]
              properties:
                score:
                  type: integer
                  minimum: 1
                  maximum: 5
      responses:
        '200':
          description: Rating updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid Token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not authorized to modify this record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Ratings]
      summary: Delete Rating
      description: Deletes a rating. Only the rating owner can delete their own rating.
      security:
        - bearerAuth: []
      responses:
        '204':
          description: Rating deleted successfully
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid Token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not authorized to modify this record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/ratings/me/enhanced:
    get:
      tags: [Ratings]
      summary: Get My Ratings (Enhanced)
      description: Returns the authenticated user's ratings (paginated), with media titles resolved from TMDB. Items whose TMDB data is unavailable (404) will have their title fall back to "Unknown Title".
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
          description: Page number.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
          description: Number of results per page.
      responses:
        '200':
          description: Successfully retrieved the authenticated user's ratings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingsEnhancedResponse'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Authenticated user not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/users/me:
    get:
      tags: [General]
      summary: Get My Profile
      description: Returns the authenticated user's profile data (everything except ratings and reviews).
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Successfully retrieved user profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfileResponse'
        '401':
          description: Missing or invalid bearer token, or authenticated user not found in local database.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/reviews/me/enhanced:
    get:
      tags: [Reviews]
      summary: Get My Reviews (Enhanced)
      description: Returns the authenticated user's reviews (paginated), with media titles resolved from TMDB. Items whose TMDB data is unavailable (404) will have their title fall back to "Unknown Title".
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
          description: Page number.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
          description: Number of results per page.
      responses:
        '200':
          description: Successfully retrieved the authenticated user's reviews.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewsEnhancedResponse'
        '401':
          description: Missing or invalid bearer token, or authenticated user not found in local database.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Failed to reach TMDB API
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /issues:
    get:
      tags: [Issues]
      summary: List Issues (Admin)
      description: |
        Returns a paginated, filterable, sortable list of issues that have not been deleted.

        **Authentication:** Requires a valid Auth² JWT with role Admin or above.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
          description: 1-based page number.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          description: Number of issues per page.
        - name: status
          in: query
          required: false
          schema:
            oneOf:
              - $ref: '#/components/schemas/IssueStatus'
              - type: array
                items:
                  $ref: '#/components/schemas/IssueStatus'
          description: Filter by one or more status values (e.g. `?status=OPEN` or `?status=OPEN&status=IN_PROGRESS`).
          explode: true
        - name: sortBy
          in: query
          required: false
          schema:
            type: string
            enum: [createdAt, status, title]
            default: createdAt
          description: Field to sort by.
        - name: sortOrder
          in: query
          required: false
          schema:
            type: string
            enum: [asc, desc]
            default: desc
          description: Sort direction.
      responses:
        '200':
          description: A paginated list of issues.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueListResponse'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Insufficient permissions (role below Admin).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [Issues]
      summary: Create Issue
      description: Creates a new issue for reporting a problem or suggestion.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, body, contact]
              properties:
                title:
                  type: string
                  example: 'Broken search'
                body:
                  type: string
                  example: 'The search functionality is not working for movies with special characters.'
                contact:
                  type: string
                  example: 'alice@example.com'
      responses:
        '201':
          description: Issue created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueCreateResponse'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /issues/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: The ID of the issue.
    get:
      tags: [Issues]
      summary: Get Issue by ID (Admin)
      description: |
        Returns a single issue by its ID.

        **Authentication:** Requires a valid Auth² JWT with role Admin or above.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Issue retrieved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueSingleResponse'
        '400':
          description: Invalid ID parameter.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Insufficient permissions (role below Admin).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Issue not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      tags: [Issues]
      summary: Patch Issue (Admin)
      description: |
        Partially updates an existing issue. Only the fields present in the
        request body are changed — omitted fields are left untouched (PATCH
        merge semantics). An empty body is a no-op (200).

        **Authentication:** Requires a valid Auth² JWT with role Admin or above.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  example: 'Corrected title'
                body:
                  type: string
                  example: 'Updated description'
                contact:
                  type: string
                  example: 'new-contact@example.com'
                status:
                  $ref: '#/components/schemas/IssueStatus'
            examples:
              triage:
                summary: Triage — move to in-progress
                value:
                  status: IN_PROGRESS
              resolve:
                summary: Mark as resolved
                value:
                  status: RESOLVED
              editDetails:
                summary: Fix a typo
                value:
                  title: 'Corrected title'
                  body: 'Updated description'
      responses:
        '200':
          description: Issue updated (or no-op for empty body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssuePatchResponse'
        '400':
          description: Validation failed (e.g. unknown status string).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Insufficient permissions (role below Admin).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Issue not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Issues]
      summary: Delete Issue (Admin)
      description: |
        Soft-deletes an issue (sets `deleted=true`). The row is preserved for
        audit purposes but excluded from all list/detail responses.

        **Authentication:** Requires a valid Auth² JWT with role Admin or above.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Issue soft-deleted. The response includes the deleted issue.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueDeleteResponse'
        '400':
          description: Invalid ID parameter.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Missing or invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Insufficient permissions (role below Admin).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Issue not found or already deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Auth²-issued JWT. Obtain a token from `https://tcss-460-iam.onrender.com`.
        **Audience:** `group-4-api`
  schemas:
    RatingEntry:
      type: object
      properties:
        id:
          type: integer
          example: 3
        score:
          type: integer
          example: 4
        createdAt:
          type: string
          format: date-time
          example: '2024-11-01T12:34:56.000Z'
        user:
          type: object
          properties:
            userId:
              type: integer
              example: 42
            username:
              type: string
              description: The user's display name. Defaults to "Username Not Found" if not set.
              example: moviecritic99
        media:
          type: object
          properties:
            tmdbId:
              type: integer
              example: 550
            type:
              type: string
              enum: [MOVIE, TV_SHOW]
              example: MOVIE
            title:
              type: string
              nullable: true
              example: Fight Club
    ReviewEntry:
      type: object
      properties:
        id:
          type: integer
          example: 7
        title:
          type: string
          nullable: true
          example: A modern classic
        body:
          type: string
          example: One of the best films ever made.
        createdAt:
          type: string
          format: date-time
          example: '2024-11-01T12:34:56.000Z'
        user:
          type: object
          properties:
            userId:
              type: integer
              example: 42
            username:
              type: string
              description: The user's display name. Defaults to "Username Not Found" if not set.
              example: moviecritic99
        media:
          type: object
          properties:
            tmdbId:
              type: integer
              example: 550
            type:
              type: string
              enum: [MOVIE, TV_SHOW]
              example: MOVIE
            title:
              type: string
              nullable: true
              example: Fight Club
    UserProfile:
      type: object
      properties:
        userId:
          type: integer
          example: 42
        username:
          type: string
          example: moviecritic99
        email:
          type: string
          example: user@example.com
        subjectId:
          type: string
          example: auth0|abc123
        role:
          type: string
          enum: [User, Moderator, Admin, SuperAdmin, Owner]
          example: User
        createdAt:
          type: string
          format: date-time
          example: '2024-11-01T12:34:56.000Z'
    UserProfileResponse:
      type: object
      properties:
        user:
          $ref: '#/components/schemas/UserProfile'
    ErrorResponse:
      type: object
      properties:
        message:
          type: string
    HeartbeatResponse:
      type: object
      properties:
        status:
          type: string
          example: 'healthy'
    ValidationError:
      type: object
      properties:
        message:
          type: string
          example: Validation failed
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: string
              message:
                type: string
    Pagination:
      type: object
      properties:
        page:
          type: integer
        limit:
          type: integer
        total:
          type: integer
        totalPages:
          type: integer
    RecentReview:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        body:
          type: string
        author:
          type: string
        createdAt:
          type: string
          format: date-time
    CommunityBlock:
      type: object
      properties:
        avgRating:
          type: number
          nullable: true
        totalRatings:
          type: integer
        totalReviews:
          type: integer
        recentReviews:
          type: array
          items:
            $ref: '#/components/schemas/RecentReview'
    MovieDetail:
      type: object
      properties:
        id:
          type: number
          example: 550
        title:
          type: string
          example: Fight Club
        overview:
          type: string
          example: A ticking-time-bomb insomniac and a slippery soap salesman channel primal male aggression into a shocking new form of therapy. Their concept catches on, with underground "fight clubs" forming in every town, until an eccentric gets in the way and ignites an out-of-control spiral toward oblivion.
        release_date:
          type: string
          example: '1999-10-15'
        poster_path:
          type: string
          example: /jSziioSwPVrOy9Yow3XhWIBDjq1.jpg
        budget:
          type: number
          example: 63000000
        genres:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                example: Drama
        community:
          $ref: '#/components/schemas/CommunityBlock'
    MovieListItem:
      type: object
      properties:
        id:
          type: number
          example: 1226863
        title:
          type: string
          example: The Super Mario Galaxy Movie
        overview:
          type: string
          example: "Having thwarted Bowser's previous plot to marry Princess Peach, Mario and Luigi now face a fresh threat in Bowser Jr..."
        release_date:
          type: string
          example: '2026-04-01'
        poster_path:
          type: string
          example: /eJGWx219ZcEMVQJhAgMiqo8tYY.jpg
        backdrop_path:
          type: string
          example: /9Z2uDYXqJrlmePznQQJhL6d92Rq.jpg
        genre_ids:
          type: array
          items:
            type: integer
          example: [10751, 35, 12, 14, 16]
        original_language:
          type: string
          example: en
    MediaSearchItem:
      type: object
      properties:
        id:
          type: number
          example: 49013
        title:
          type: string
          example: Cars 2
        release_date:
          type: string
          example: '2011-06-11'
        poster_path:
          type: string
          example: /okIz1HyxeVOMzYwwHUjH2pHi74I.jpg
    SearchResponse:
      type: object
      properties:
        page:
          type: integer
          example: 1
        results:
          type: array
          items:
            $ref: '#/components/schemas/MediaSearchItem'
    TVShowDetail:
      type: object
      properties:
        id:
          type: number
          example: 1396
        name:
          type: string
          example: Breaking Bad
        overview:
          type: string
          example: Walter White, a New Mexico chemistry teacher, is diagnosed with Stage III cancer and given a prognosis of only two years left to live. He becomes filled with a sense of fearlessness and an unrelenting desire to secure his family's financial future at any cost as he enters the dangerous world of drugs and crime.
        first_air_date:
          type: string
          example: 2008-01-20
        poster_path:
          type: string
          example: /ztkUQFLlC19CCMYHW9o1zWhJRNq.jpg
        status:
          type: string
          example: Ended
        genres:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                example: Drama
        community:
          $ref: '#/components/schemas/CommunityBlock'
    TVShowListItem:
      type: object
      properties:
        id:
          type: number
          example: 76479
        name:
          type: string
          example: The Boys
        overview:
          type: string
          example: A group of vigilantes known informally as "The Boys" set out to take down corrupt superheroes with no more than blue-collar grit and a willingness to fight dirty.
        first_air_date:
          type: string
          example: '2019-07-25'
        poster_path:
          type: string
          example: /in1R2dDc421JxsoRWaIIAqVI2KE.jpg
        backdrop_path:
          type: string
          example: /bq28ajZaoMyzEIm6REelqyqtEDZ.jpg
        genre_ids:
          type: array
          items:
            type: integer
          example: [10765, 10759]
        original_language:
          type: string
          example: en
    ReviewResponse:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        mediaId:
          type: integer
        title:
          type: string
          nullable: true
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ReviewWithRelations:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        mediaId:
          type: integer
        title:
          type: string
          nullable: true
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        user:
          type: object
          properties:
            username:
              type: string
        media:
          type: object
          properties:
            tmdbId:
              type: integer
            type:
              type: string
              enum: [MOVIE, TV_SHOW]
    ReviewListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ReviewWithRelations'
        pagination:
          $ref: '#/components/schemas/Pagination'
    RatingResponse:
      type: object
      properties:
        id:
          type: integer
          example: 1
        score:
          type: integer
          example: 5
        userId:
          type: integer
          example: 42
        mediaId:
          type: integer
          example: 105
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    RatingWithRelations:
      type: object
      properties:
        id:
          type: integer
          example: 1
        score:
          type: integer
          example: 5
        userId:
          type: integer
          example: 42
        mediaId:
          type: integer
          example: 105
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        user:
          type: object
          properties:
            username:
              type: string
              example: moviecritic99
        media:
          type: object
          properties:
            tmdbId:
              type: integer
              example: 550
            type:
              type: string
              example: MOVIE
    RatingListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/RatingWithRelations'
        pagination:
          $ref: '#/components/schemas/Pagination'
    Issue:
      type: object
      properties:
        id:
          type: integer
          example: 1
        title:
          type: string
          example: 'Broken search'
        body:
          type: string
          example: 'Search fails for movies with special characters.'
        contact:
          type: string
          example: 'user@example.com'
        status:
          $ref: '#/components/schemas/IssueStatus'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    IssueStatus:
      type: string
      enum: [OPEN, IN_PROGRESS, RESOLVED, WONT_FIX]
      example: OPEN
    IssueListResponse:
      type: object
      properties:
        issues:
          type: array
          items:
            $ref: '#/components/schemas/Issue'
        pagination:
          $ref: '#/components/schemas/Pagination'
    IssueSingleResponse:
      type: object
      properties:
        issue:
          $ref: '#/components/schemas/Issue'
    IssueCreateResponse:
      type: object
      properties:
        message:
          type: string
          example: 'Issue created'
        issue:
          $ref: '#/components/schemas/Issue'
    IssuePatchResponse:
      type: object
      properties:
        message:
          type: string
          example: 'Issue updated'
        issue:
          $ref: '#/components/schemas/Issue'
    IssueDeleteResponse:
      type: object
      properties:
        message:
          type: string
          example: 'Issue deleted'
        issue:
          $ref: '#/components/schemas/Issue'
    FeaturedMovie:
      type: object
      properties:
        id:
          type: number
          example: 1226863
        title:
          type: string
          example: The Super Mario Galaxy Movie
        overview:
          type: string
          example: "Having thwarted Bowser's previous plot to marry Princess Peach, Mario and Luigi now face a fresh threat in Bowser Jr..."
        release_date:
          type: string
          example: '2026-04-01'
        poster_path:
          type: string
          example: /eJGWx219ZcEMVQJhAgMiqo8tYY.jpg
        backdrop_path:
          type: string
          example: /9Z2uDYXqJrlmePznQQJhL6d92Rq.jpg
        genre_ids:
          type: array
          items:
            type: integer
          example: [10751, 35, 12, 14, 16]
        original_language:
          type: string
          example: en
        communityStats:
          oneOf:
            - type: object
              required: [rankingMetric, totalReviews]
              properties:
                rankingMetric:
                  type: string
                  enum: [most-reviewed]
                totalReviews:
                  type: integer
                  example: 10
            - type: object
              required: [rankingMetric, totalRatings]
              properties:
                rankingMetric:
                  type: string
                  enum: [top-rated]
                avgScore:
                  type: number
                  nullable: true
                  example: 4.5
                totalRatings:
                  type: integer
                  example: 20
    FeaturedTVShow:
      type: object
      properties:
        id:
          type: number
          example: 76479
        name:
          type: string
          example: The Boys
        overview:
          type: string
          example: A group of vigilantes known informally as "The Boys" set out to take down corrupt superheroes with no more than blue-collar grit and a willingness to fight dirty.
        first_air_date:
          type: string
          example: '2019-07-25'
        poster_path:
          type: string
          example: /in1R2dDc421JxsoRWaIIAqVI2KE.jpg
        backdrop_path:
          type: string
          example: /bq28ajZaoMyzEIm6REelqyqtEDZ.jpg
        genre_ids:
          type: array
          items:
            type: integer
          example: [10765, 10759]
        original_language:
          type: string
          example: en
        communityStats:
          oneOf:
            - type: object
              required: [rankingMetric, totalReviews]
              properties:
                rankingMetric:
                  type: string
                  enum: [most-reviewed]
                totalReviews:
                  type: integer
                  example: 10
            - type: object
              required: [rankingMetric, totalRatings]
              properties:
                rankingMetric:
                  type: string
                  enum: [top-rated]
                avgScore:
                  type: number
                  nullable: true
                  example: 4.5
                totalRatings:
                  type: integer
                  example: 20
    ReviewsEnhancedResponse:
      type: object
      properties:
        reviews:
          type: array
          items:
            $ref: '#/components/schemas/ReviewEntry'
    RatingsEnhancedResponse:
      type: object
      properties:
        ratings:
          type: array
          items:
            $ref: '#/components/schemas/RatingEntry'
